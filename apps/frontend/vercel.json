{
  "buildCommand": "cd ../.. && npm install -g pnpm && pnpm install && cd apps/frontend && pnpm run build",
  "installCommand": "echo 'handled in buildCommand'",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
